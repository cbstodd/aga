<%= form_for(@aga_employee, :html => {:multipart => true}) do |f| %>
  <% if @aga_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aga_employee.errors.count, "error") %> prohibited this aga_employee from being saved:</h2>

      <ul>
      <% @aga_employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :job_title %><br>
    <%= f.text_area :job_title %>
  </div>
  <div class="field">
    <%= f.label :job_description %><br>
    <%= f.text_area :job_description %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.text_area :phone_number %>
  </div>

  <div class="field">
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
